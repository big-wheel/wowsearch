/**
 * @file runPQueue
 * @author Cuttle Cong
 * @date 2019/10/6
 * @description
 */

const runPQueue = require('../lib/queueFn')

describe('runPQueue', function () {
  it('should runPQueue', () => {
    expect(1 + 2).toBe(3)
  })
})
